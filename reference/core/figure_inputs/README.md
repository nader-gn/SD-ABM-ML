# Derived figure data

These files are generated by upstream core-model steps and then consumed by plotting scripts. They are stored here only as frozen verification targets.

Fresh equivalents are generated at:

`reproduced/workspace/computational_bundle/figure_inputs/`

They are not primary model inputs and are never read by the production workflow.
